import 'package:flutter/material.dart';
class RatingsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(

      child: Center(child: Text('Ratings Tab')),

    );
  }
}